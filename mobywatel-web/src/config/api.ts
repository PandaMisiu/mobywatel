// API configuration
// Since nginx serves both frontend and backend under the same origin,
// we can use relative URLs (same origin, no CORS issues)
const API_BASE_URL = '';

export { API_BASE_URL };
